# 🚀 Быстрый старт с Groq API

## Шаг 1: Вставьте ваш Groq API ключ

### Вариант A: Простой способ (рекомендуется)

1. Откройте файл `setup_groq_simple.sh`
2. Найдите строку:
   ```bash
   GROQ_API_KEY="ВСТАВЬТЕ_ВАШ_GROQ_API_KEY_СЮДА"
   ```
3. Замените `ВСТАВЬТЕ_ВАШ_GROQ_API_KEY_СЮДА` на ваш реальный ключ
4. Запустите скрипт:
   ```bash
   ./setup_groq_simple.sh
   ```

### Вариант B: Через .env файл

1. Скопируйте пример:
   ```bash
   cp .env.example .env
   ```
2. Откройте `.env` и вставьте ваш ключ:
   ```
   GROQ_API_KEY="ваш_ключ_здесь"
   ```
3. Загрузите переменные:
   ```bash
   source .env
   ```

### Вариант C: Интерактивный скрипт

Просто запустите:
```bash
./setup_groq.sh
```

И следуйте инструкциям.

---

## Шаг 2: Используйте Groq

### Через Streamlit:
```bash
./run_streamlit.sh
```
В интерфейсе выберите **groq** в разделе "Режим генерации"

### Через CLI:
```bash
python3 -m src.app.cli \
  --input data/synthetic_users_20.csv \
  --goal активация \
  --channel push \
  --llm-mode groq
```

---

## ✅ Проверка

Проверьте, что ключ установлен:
```bash
echo $GROQ_API_KEY
```

Если видите ваш ключ — всё готово! 🎉

---

## 🔧 Постоянная настройка

Чтобы ключ работал всегда, добавьте в `~/.zshrc`:
```bash
export GROQ_API_KEY="ваш_ключ"
```

Затем:
```bash
source ~/.zshrc
```

