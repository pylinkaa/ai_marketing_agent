# 🚀 Инструкция по загрузке проекта на GitHub

## Шаг 1: Подготовка проекта

### 1.1 Убедитесь, что все нужные файлы на месте

Проверьте структуру проекта:
```
ai_marketing_agent/
├── src/              # Исходный код
├── configs/          # Конфигурационные файлы
├── data/             # Тестовые данные (только примеры)
├── tests/            # Тесты
├── requirements.txt  # Зависимости
├── README.md        # Основная документация
└── .gitignore       # Игнорируемые файлы
```

### 1.2 Удалите чувствительные данные

✅ Убедитесь, что в `.gitignore` есть:
- `.env` файлы
- `outputs/` директория
- API ключи в скриптах

✅ Проверьте, что в `setup_groq_simple.sh` и `EXPORT_GROQ_KEY.sh` нет реальных ключей (или они в `.gitignore`)

---

## Шаг 2: Инициализация Git репозитория

### 2.1 Если Git еще не инициализирован

```bash
cd /Users/polina/Documents/ai_marketing_agent
git init
```

### 2.2 Проверьте статус

```bash
git status
```

Убедитесь, что `.env`, `outputs/`, и другие чувствительные файлы не попадают в коммит.

---

## Шаг 3: Первый коммит

### 3.1 Добавьте все файлы

```bash
git add .
```

### 3.2 Проверьте, что будет закоммичено

```bash
git status
```

**ВАЖНО:** Убедитесь, что НЕ добавляются:
- ❌ `.env` файлы
- ❌ `outputs/` директория
- ❌ Файлы с реальными API ключами
- ❌ `__pycache__/` директории
- ❌ `.DS_Store` файлы

### 3.3 Создайте первый коммит

```bash
git commit -m "Initial commit: AI Marketing Agent with Streamlit frontend"
```

---

## Шаг 4: Создание репозитория на GitHub

### 4.1 Создайте новый репозиторий

1. Зайдите на [GitHub.com](https://github.com)
2. Нажмите **"+"** → **"New repository"**
3. Заполните:
   - **Repository name:** `ai-marketing-agent` (или другое название)
   - **Description:** "AI-powered marketing agent for personalized e-commerce communications"
   - **Visibility:** Public или Private (на ваше усмотрение)
   - ❌ **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license" (у нас уже есть)

4. Нажмите **"Create repository"**

### 4.2 Скопируйте URL репозитория

После создания GitHub покажет URL, например:
```
https://github.com/your-username/ai-marketing-agent.git
```

---

## Шаг 5: Подключение локального репозитория к GitHub

### 5.1 Добавьте remote

```bash
git remote add origin https://github.com/your-username/ai-marketing-agent.git
```

Замените `your-username` на ваш GitHub username.

### 5.2 Проверьте подключение

```bash
git remote -v
```

Должно показать:
```
origin  https://github.com/your-username/ai-marketing-agent.git (fetch)
origin  https://github.com/your-username/ai-marketing-agent.git (push)
```

---

## Шаг 6: Загрузка кода на GitHub

### 6.1 Переименуйте ветку в main (если нужно)

```bash
git branch -M main
```

### 6.2 Загрузите код

```bash
git push -u origin main
```

Если GitHub попросит авторизацию:
- Используйте Personal Access Token (не пароль)
- Или настройте SSH ключи

---

## Шаг 7: Проверка

1. Обновите страницу репозитория на GitHub
2. Убедитесь, что все файлы загружены
3. Проверьте, что `.env` и `outputs/` НЕ видны в репозитории

---

## 🔒 Безопасность

### Что НИКОГДА не должно попасть в Git:

- ❌ Реальные API ключи
- ❌ `.env` файлы с ключами
- ❌ Персональные данные пользователей
- ❌ Пароли и токены

### Если случайно закоммитили ключ:

1. **Немедленно** удалите ключ из репозитория
2. Сгенерируйте новый ключ в сервисе (Groq/OpenAI/HF)
3. Обновите `.gitignore`
4. Используйте `git filter-branch` или `BFG Repo-Cleaner` для удаления из истории

---

## 📝 Дополнительные настройки (опционально)

### Добавьте описание репозитория

На странице репозитория на GitHub:
1. Нажмите ⚙️ (Settings)
2. Scroll down до "About"
3. Добавьте описание и теги

### Создайте Release

После первого коммита можно создать первый релиз:
1. Нажмите "Releases" → "Create a new release"
2. Tag: `v1.0.0`
3. Title: `v1.0.0 - Initial Release`
4. Description: опишите основные функции

---

## 🆘 Полезные команды

```bash
# Проверить статус
git status

# Посмотреть изменения
git diff

# Отменить изменения в файле (до add)
git checkout -- filename

# Удалить файл из индекса (но оставить локально)
git rm --cached filename

# Посмотреть историю коммитов
git log

# Обновить локальный репозиторий
git pull origin main

# Загрузить изменения
git push origin main
```

---

## ✅ Чеклист перед загрузкой

- [ ] Все API ключи удалены из кода
- [ ] `.env` файлы в `.gitignore`
- [ ] `outputs/` в `.gitignore`
- [ ] `__pycache__/` в `.gitignore`
- [ ] README.md обновлен
- [ ] requirements.txt актуален
- [ ] Тесты работают (опционально)
- [ ] Код проверен на ошибки

---

Готово! 🎉 Ваш проект теперь на GitHub!

