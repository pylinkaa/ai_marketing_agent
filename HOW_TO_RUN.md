# 🚀 Как запустить пайплайн

## Шаг 1: Экспортируйте Groq API ключ

В терминале выполните:
```bash
source EXPORT_GROQ_KEY.sh
```

Или напрямую:
```bash
export GROQ_API_KEY="YOUR_GROQ_API_KEY_HERE"
```

## Шаг 2: Выберите способ запуска

### Вариант A: Через Streamlit (веб-интерфейс) - РЕКОМЕНДУЕТСЯ

```bash
# 1. Экспортируйте ключ (если еще не сделали)
source EXPORT_GROQ_KEY.sh

# 2. Запустите Streamlit
./run_streamlit.sh
# или
python3 -m streamlit run src/app/streamlit_app.py
```

В браузере:
1. Загрузите CSV файл (или используйте файл по умолчанию)
2. Выберите параметры кампании (цель, канал, стиль)
3. Выберите режим сегментации (rule или ml)
4. **Выберите "groq" в разделе "Режим генерации"**
5. Нажмите "🚀 Запустить пайплайн"

---

### Вариант B: Через командную строку (CLI)

```bash
# 1. Экспортируйте ключ (если еще не сделали)
source EXPORT_GROQ_KEY.sh

# 2. Запустите пайплайн
python3 -m src.app.cli \
  --input data/synthetic_users_20.csv \
  --goal активация \
  --channel push \
  --style дружелюбный \
  --mode rule \
  --llm-mode groq
```

**Параметры:**
- `--input` - путь к CSV файлу с данными
- `--goal` - цель кампании: `активация`, `реактивация`, `удержание`, `upsell`, `промо`, `сервис`
- `--channel` - канал: `push`, `email`, `inapp`
- `--style` - стиль: `дружелюбный`, `формальный`, `срочный`, `информативный`
- `--mode` - режим сегментации: `rule` (правила) или `ml` (машинное обучение)
- `--llm-mode` - режим генерации: `mock`, `groq`, `hf`, `openai`

**Примеры:**

```bash
# Реактивация через email
python3 -m src.app.cli \
  --input data/synthetic_users_20.csv \
  --goal реактивация \
  --channel email \
  --style дружелюбный \
  --llm-mode groq

# Удержание через push с ML сегментацией
python3 -m src.app.cli \
  --input data/synthetic_users_100.csv \
  --goal удержание \
  --channel push \
  --mode ml \
  --llm-mode groq
```

---

## Результаты

После выполнения пайплайн создаст файлы в директории `outputs/`:
- `messages_YYYYMMDD_HHMMSS.csv` - сгенерированные сообщения
- `messages_YYYYMMDD_HHMMSS.json` - те же данные в JSON
- `metrics_YYYYMMDD_HHMMSS.json` - метрики сегментации

---

## Проверка подключения

Перед запуском пайплайна можно проверить подключение к Groq:

```bash
source EXPORT_GROQ_KEY.sh
python3 test_groq.py
```

Если тест проходит успешно - всё готово! 🎉

---

## Постоянная настройка (опционально)

Чтобы не экспортировать ключ каждый раз, добавьте в `~/.zshrc`:

```bash
export GROQ_API_KEY="YOUR_GROQ_API_KEY_HERE"
```

Затем:
```bash
source ~/.zshrc
```

