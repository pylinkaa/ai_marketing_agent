# 🚀 AI Marketing Agent

## 💡 Зачем он нужен?

Представьте: у вас есть база из тысяч пользователей e-commerce платформы. Каждому нужно отправить персонализированное маркетинговое сообщение. 

**Проблема:** 
- ❌ Ручная работа займет недели
- ❌ Шаблонные сообщения не работают
- ❌ Нет понимания, кого и как сегментировать
- ❌ Сложно масштабировать на большие объемы

**Решение:**
- ✅ Автоматическая сегментация пользователей (rule-based или ML)
- ✅ Генерация персонализированных сообщений через LLM
- ✅ Поддержка разных каналов (Push, Email, In-app)
- ✅ Готовые сообщения для каждого пользователя за минуты

---

## 🎯 Что было сделано

### Шаг 1: Загрузка и обработка данных

**Что сделано:**
- Реализована загрузка CSV файлов с данными пользователей
- Поддержка различных форматов и автоматическая валидация
- Feature engineering: извлечение признаков для ML-модели

**Как запустить:**
```bash
# 1. Перейдите в директорию проекта
cd /Users/polina/Documents/ai_marketing_agent

# 2. Установите зависимости (один раз)
python3 -m pip install -r requirements.txt
```

**Подготовьте свой CSV файл:**
Ваш CSV должен содержать колонки с данными пользователей (например, `user_id`, `purchase_count`, `ltv`, `last_active_date` и т.д.). Примеры файлов находятся в папке `data/`.

---

### Шаг 2: Сегментация пользователей

**Что сделано:**
- **Rule-based сегментация:** Разделение пользователей по правилам (новые, VIP, спящие и т.д.)
- **ML-based сегментация:** Обучение RandomForest модели для предсказания сегментов
- Автоматическое создание профилей сегментов без PII (приватность данных)

**Как запустить с rule-based сегментацией:**
```bash
python3 -m src.app.cli \
  --input data/synthetic_users_20.csv \
  --goal активация \
  --channel push \
  --style дружелюбный \
  --mode rule \
  --llm-mode mock
```

**Как запустить с ML-сегментацией:**
```bash
python3 -m src.app.cli \
  --input data/synthetic_users_100.csv \
  --goal удержание \
  --channel email \
  --style дружелюбный \
  --mode ml \
  --llm-mode groq
```

> 💡 **Совет:** Начните с `--mode rule` для быстрого тестирования, затем переходите к `--mode ml` для более точной сегментации.

---

### Шаг 3: Генерация персонализированных сообщений

**Что сделано:**
- Интеграция с несколькими LLM провайдерами:
  - **Groq** (бесплатный, быстрый) ⭐ Рекомендуется
  - **Hugging Face** (бесплатный)
  - **OpenAI** (платный, высокое качество)
  - **Mock режим** (для тестирования без API)
- Промпт-инжиниринг: создание контекстных промптов на основе профиля сегмента
- Постобработка: адаптация сообщений под канал (длина, стиль)

**Как настроить API ключ (для Groq):**
```bash
# Получите бесплатный ключ на https://console.groq.com/
export GROQ_API_KEY="ваш_ключ_здесь"
```

**Как запустить с генерацией через Groq:**
```bash
# 1. Экспортируйте ключ
export GROQ_API_KEY="ваш_ключ"

# 2. Запустите пайплайн
python3 -m src.app.cli \
  --input data/synthetic_users_20.csv \
  --goal активация \
  --channel push \
  --style дружелюбный \
  --llm-mode groq
```

**Или используйте mock режим (без API):**
```bash
python3 -m src.app.cli \
  --input data/synthetic_users_20.csv \
  --goal активация \
  --channel push \
  --style дружелюбный \
  --llm-mode mock
```

---

### Шаг 4: Веб-интерфейс для удобной работы

**Что сделано:**
- Streamlit приложение с современным UI
- Визуализация метрик сегментации
- Просмотр и скачивание сгенерированных сообщений
- Интерактивный выбор параметров кампании

**Как запустить веб-интерфейс:**
```bash
# 1. Экспортируйте ключ (если используете Groq)
export GROQ_API_KEY="ваш_ключ"

# 2. Запустите Streamlit
python3 -m streamlit run src/app/streamlit_app.py

# Или используйте скрипт
./run_streamlit.sh
```

Откроется браузер на `http://localhost:8501` 🎉

---

## 🚀 Быстрый старт (3 команды)

```bash
# 1. Установите зависимости
python3 -m pip install -r requirements.txt

# 2. Экспортируйте API ключ (опционально, для Groq)
export GROQ_API_KEY="ваш_ключ"

# 3. Запустите веб-интерфейс
python3 -m streamlit run src/app/streamlit_app.py
```

Или через командную строку:
```bash
python3 -m src.app.cli \
  --input data/synthetic_users_20.csv \
  --goal активация \
  --channel push \
  --style дружелюбный \
  --llm-mode groq
```

---

## 📋 Параметры запуска

### Цели кампании (`--goal`)
- `активация` - привлечение новых пользователей
- `реактивация` - возврат неактивных пользователей
- `удержание` - удержание активных клиентов
- `upsell` - продажа более дорогих товаров
- `промо` - промо-акции и скидки
- `сервис` - сервисные сообщения

### Каналы (`--channel`)
- `push` - Push-уведомления (до 100 символов)
- `email` - Email рассылки (до 500 символов)
- `inapp` - In-app сообщения (до 300 символов)

### Стили (`--style`)
- `дружелюбный` - неформальный, дружелюбный тон
- `формальный` - деловой стиль
- `срочный` - создает ощущение срочности
- `информативный` - информационный стиль

### Режимы сегментации (`--mode`)
- `rule` - правило-основанная сегментация (быстро, не требует обучения)
- `ml` - машинное обучение (точнее, требует данные с метками)

### Режимы генерации (`--llm-mode`)
- `mock` - тестовый режим без API (быстро, для проверки)
- `groq` - Groq API (бесплатный, быстрый) ⭐
- `hf` - Hugging Face API (бесплатный)
- `openai` - OpenAI API (платный, высокое качество)

---

## 📊 Результаты работы

После выполнения пайплайн создает файлы в папке `outputs/`:

- **`messages_YYYYMMDD_HHMMSS.csv`** - таблица со всеми сгенерированными сообщениями
- **`messages_YYYYMMDD_HHMMSS.json`** - те же данные в JSON формате
- **`metrics_YYYYMMDD_HHMMSS.json`** - метрики сегментации (размеры сегментов, точность и т.д.)

Каждая строка в CSV содержит:
- `user_id` - ID пользователя
- `segment_label` - метка сегмента
- `goal` - цель кампании
- `channel` - канал коммуникации
- `message` - готовое персонализированное сообщение

---

## 🏗️ Архитектура проекта

```
ai_marketing_agent/
├── src/
│   ├── app/              # Интерфейсы (CLI, Streamlit, Interactive)
│   ├── core/             # Основной пайплайн
│   ├── features/          # Feature engineering
│   ├── llm/               # LLM клиенты (Groq, HF, OpenAI)
│   ├── prompting/         # Промпты и шаблоны
│   ├── segmentation/     # Сегментация (rule-based, ML)
│   └── utils/             # Утилиты (загрузка/сохранение)
├── configs/               # Конфигурационные файлы
├── data/                  # Тестовые данные
├── outputs/               # Результаты (создается автоматически)
└── tests/                 # Тесты
```

---

## 🔧 Конфигурация

Основные настройки находятся в `configs/default.yaml`:
- Параметры сегментации (пороги для rule-based, параметры ML-модели)
- Настройки LLM провайдеров (модели, температура, таймауты)
- Ограничения длины для каналов
- Feature engineering параметры

---

## 🧪 Тестирование

```bash
# Запустить unit-тесты
pytest tests/

# Проверить подключение к Groq API
export GROQ_API_KEY="ваш_ключ"
python3 test_groq.py
```

---

## 📖 Дополнительная документация

- [HOW_TO_RUN.md](HOW_TO_RUN.md) - Подробная инструкция по запуску
- [API_KEYS.md](API_KEYS.md) - Как получить API ключи для разных провайдеров
- [QUICK_START_GROQ.md](QUICK_START_GROQ.md) - Быстрый старт с Groq
- [GITHUB_SETUP.md](GITHUB_SETUP.md) - Настройка GitHub репозитория

---

## 🔒 Безопасность

- ✅ Все API ключи через переменные окружения
- ✅ Никакой PII в сегментах (только агрегированные данные)
- ✅ `.env` файлы в `.gitignore`
- ❌ **НЕ коммитьте** файлы с реальными ключами

---

## 💡 Примеры использования

### Пример 1: Реактивация через Email
```bash
python3 -m src.app.cli \
  --input data/synthetic_users_20.csv \
  --goal реактивация \
  --channel email \
  --style дружелюбный \
  --llm-mode groq
```

### Пример 2: Удержание с ML-сегментацией
```bash
python3 -m src.app.cli \
  --input data/synthetic_users_100.csv \
  --goal удержание \
  --channel push \
  --mode ml \
  --llm-mode groq
```

### Пример 3: Интерактивный режим
```bash
python3 -m src.app.cli --interactive
```
<<<<<<< HEAD

---

## 🎉 Готово!

Теперь у вас есть полноценный AI Marketing Agent, который:
- ✅ Автоматически сегментирует пользователей
- ✅ Генерирует персонализированные сообщения
- ✅ Работает с разными каналами и целями
- ✅ Масштабируется на большие объемы данных
