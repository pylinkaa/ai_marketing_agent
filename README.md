# 🚀 AI Marketing Agent

ИИ-агент для генерации персонализированных маркетинговых сообщений на основе сегментации пользователей e-commerce платформы.

## ✨ Особенности

- 🎯 **Персонализация**: Генерация сообщений на основе сегментации пользователей
- 🔒 **Privacy-by-design**: Никакой PII, только агрегированные данные сегментов
- 🤖 **Множество LLM**: Поддержка Groq (бесплатный), Hugging Face, OpenAI
- 📊 **Сегментация**: Rule-based и ML-based сегментация
- 🎨 **Streamlit UI**: Современный веб-интерфейс для работы с агентом
- 📱 **Множество каналов**: Push, Email, In-app сообщения

## 🏗️ Структура проекта

```
ai_marketing_agent/
├── src/                    # Исходный код
│   ├── app/               # Интерфейсы (CLI, Streamlit)
│   ├── core/              # Основной пайплайн
│   ├── features/          # Feature engineering
│   ├── llm/               # LLM клиенты (Groq, HF, OpenAI)
│   ├── prompting/         # Промпты и шаблоны
│   ├── segmentation/      # Сегментация (rule-based, ML)
│   └── utils/             # Утилиты
├── configs/               # Конфигурационные файлы
├── data/                  # Тестовые данные
├── tests/                 # Тесты
└── outputs/               # Результаты (создается автоматически)
```

## 📦 Установка

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/your-username/ai-marketing-agent.git
cd ai-marketing-agent
```

2. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

3. **Настройте API ключи (опционально):**
```bash
# Для Groq (рекомендуется - бесплатный и быстрый)
export GROQ_API_KEY="your_groq_key"

# Или создайте .env файл
cp .env.example .env
# Отредактируйте .env и добавьте свои ключи
```

## 🚀 Быстрый старт

### Вариант 1: Streamlit (веб-интерфейс) - РЕКОМЕНДУЕТСЯ

```bash
# Экспортируйте ключ (если используете Groq)
export GROQ_API_KEY="your_key"

# Запустите Streamlit
python3 -m streamlit run src/app/streamlit_app.py
# или
./run_streamlit.sh
```

Откроется браузер на `http://localhost:8501`

### Вариант 2: Командная строка

```bash
python3 -m src.app.cli \
  --input data/synthetic_users_20.csv \
  --goal активация \
  --channel push \
  --style дружелюбный \
  --llm-mode groq
```

## 📖 Документация

- [HOW_TO_RUN.md](HOW_TO_RUN.md) - Подробная инструкция по запуску
- [API_KEYS.md](API_KEYS.md) - Как получить API ключи
- [QUICK_START_GROQ.md](QUICK_START_GROQ.md) - Быстрый старт с Groq
- [GITHUB_SETUP.md](GITHUB_SETUP.md) - Как загрузить на GitHub

## 🎯 Использование

### Параметры кампании

- **Цели**: `активация`, `реактивация`, `удержание`, `upsell`, `промо`, `сервис`
- **Каналы**: `push`, `email`, `inapp`
- **Стили**: `дружелюбный`, `формальный`, `срочный`, `информативный`
- **Режимы сегментации**: `rule` (правила), `ml` (машинное обучение)
- **Режимы генерации**: `mock`, `groq`, `hf`, `openai`

### Примеры

```bash
# Реактивация через email
python3 -m src.app.cli \
  --input data/synthetic_users_20.csv \
  --goal реактивация \
  --channel email \
  --llm-mode groq

# Удержание с ML сегментацией
python3 -m src.app.cli \
  --input data/synthetic_users_100.csv \
  --goal удержание \
  --channel push \
  --mode ml \
  --llm-mode groq
```

## 🔧 Конфигурация

Основные настройки в `configs/default.yaml`:
- Параметры сегментации
- Настройки LLM провайдеров
- Ограничения длины для каналов

## 📊 Результаты

После выполнения пайплайн создает файлы в `outputs/`:
- `messages_*.csv` - сгенерированные сообщения
- `metrics_*.json` - метрики сегментации

## 🧪 Тестирование

```bash
# Запустить тесты
pytest tests/

# Проверить подключение к Groq
python3 test_groq.py
```

## 🔒 Безопасность

- ❌ **НЕ коммитьте** `.env` файлы с реальными ключами
- ❌ **НЕ коммитьте** `outputs/` директорию
- ✅ Используйте `.env.example` как шаблон
- ✅ Все API ключи через переменные окружения

## 📝 Лицензия

[Укажите вашу лицензию]

## 🤝 Вклад

Pull requests приветствуются! Для больших изменений сначала откройте issue.

## 📧 Контакты

[Ваши контакты]

---

**Сделано с ❤️ для персонализированного маркетинга**
